import { ImageSourcePropType, ImageURISource } from 'react-native';

export class Images {
  public static Incoming: ImageURISource = require('./svg/incoming.svg');
  public static Outgoing: ImageURISource = require('./svg/outgoing.svg');
}
